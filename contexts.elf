ctx : type.
%name ctx G.

% open terms
otm : ctx -> type.

ctx/nil : ctx.
%abbrev · = ctx/nil.

ctx/snoc : {G:ctx} otm G -> ctx.
%abbrev , = ctx/snoc. %infix left 9 ,.

! : tm -> otm ·. %prefix 10 !.
@ : (tm -> otm G) -> otm (G , A). %prefix 10 @.
